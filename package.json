{
  "name": "collabsphere",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix backend install",
    "build": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix backend install",
    "start": "npm --prefix backend start"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
